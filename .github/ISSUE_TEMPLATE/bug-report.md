---
name: Bug report
about: Something in the crate behaves incorrectly
title: "[Bug] "
labels: bug
---

## Current behavior

<!-- What happens now. Include the payload or filter expression if there is
     one, and the exact error text. A minimal reproduction is worth more than
     a description. -->

## Expected behavior

<!-- What should happen instead, and why — ideally with the RFC section that
     says so. The RFC texts are in docs/rfcs/. -->

## Reproduction

```rust
// Smallest code that shows the problem.
```

## Environment

- `scim_v2` version:
- Rust version (`rustc --version`):
- Features enabled (default, or which subset):

## Provider

<!-- If this came from a real SCIM provider (Entra, Okta, JumpCloud, Google,
     GitHub…), say which. A sanitized copy of the payload is the single most
     useful thing you can attach — it can become a test fixture. -->
